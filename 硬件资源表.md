# 硬件资源表

## 芯片与开发环境

- **设备**：MaixCAM2
- **开发框架**：MaixPy / Python 3
- **图像处理**：OpenCV、NumPy
- **相机分析画面**：默认1280 x 960；创建失败时回退640 x 480并显示`RES LOW`
- **屏幕显示画面**：固定640 x 480；分析坐标按比例映射到显示坐标
- **安装方式**：相机与黑色 A4 纸位置固定
- **机械范围**：XY 运动范围 230 x 230 mm，高度 100 mm
- **有效纸面**：竖放默认上下各舍弃33.5mm得到210×230mm；横放默认左右各舍弃33.5mm得到230×210mm
- **最后更新**：2026-07-31

## 集成硬件资源

| 资源 | 用途 | 接口 | 状态 | 冲突检测 | 备注 |
|---|---|---|---|---|---|
| 板载相机 | 采集黑纸与白色碎片 | MaixPy `camera` | 已使用 | 无外部引脚分配 | 固定安装 |
| 板载屏幕 | 显示识别与调参画面 | MaixPy `display` | 已使用 | 无外部引脚分配 | 640 x 480 坐标布局 |
| 板载触摸 | 模式切换与现场调参 | MaixPy `touchscreen` | 已使用 | 无外部引脚分配 | 松开触发单次点击 |
| 持久存储 | 保存视觉参数 | `/root/maixcam2_puzzle/vision_settings.json` | 已使用 | 不适用 | JSON 原子替换 |
| A版持久存储 | 保存四边形掩膜版参数 | `/root/maixcam2_puzzle_A/vision_settings.json` | PC已验证 | 不适用 | 与稳定版、B版隔离 |
| A版布局存储 | 保存K1～K4形状与100×60mm正确布局 | `/root/maixcam2_puzzle_A/known_templates.json` | PC已验证 | 不适用 | 不依赖MaixVision `/tmp`目录 |
| A版UNKNOWN求解调度 | WHITE依次`GRAPH_AUTO → FOUR_FAST → FALLBACK`；CARD保留纹理择优 | 同一锁定快照＋协作式时间片 | PC已验证 | 不使用线程或外部引脚 | 三路径共享8秒活动/30秒墙钟、24ms/64单元；FOUR_FAST仅四片 |
| A版四片分段快路径 | 支持短边贴长边局部的T形接缝 | NumPy小多边形＋OpenCV几何求交 | PC已验证 | 不增加线程、引脚或持久存储 | Beam 32、片对关系6、工作单元上限2400；失败自动转FALLBACK |
| A版UNKNOWN双几何 | 完整轮廓执行重叠、硬验收、机械中心和绘制；每片最多3个候选只提出接缝刚体位姿 | NumPy毫米多边形纯函数 | PC已验证 | 不修改KNOWN模板或持久存储 | 原轮廓质心；99%面积/0.75mm高保真压缩；候选为20mm基准、14mm下限、96%面积、3mm偏差 |
| A版控制台诊断 | AUTO、PIECE、SHAPE、TIER、GRAPH、FOUR_FAST、FALLBACK、ACCEPT和终态一次性日志 | MaixVision标准输出 | PC已验证 | 无外部引脚或存储占用 | 默认开启；关闭后不构造明细；CLEAN仅为旧算法对照预览，不参与生产求解 |
| A版KNOWN登记 | 下半区正确100×60mm布局同步保存 | 固定最多24种模板分配 | PC已验证 | 不启动UNKNOWN任务 | PC平均约3.964ms |
| A版锁定与纸面视图 | 第3稳定帧锁定单次碎片快照；正常页只显示标定A4纸面 | 主循环快照状态＋显示用Homography | PC已验证 | 不增加相机、触摸或存储资源 | LOCKED后跳过重复分割；检测仍限于纸内黄色机械有效区；超时需按钮手动重拍 |
| A版START确认门 | 正常页按当前KNOWN/UNKNOWN和WHITE/CARD选择启动一次采集 | 640×480触摸按钮＋`capture_armed`状态 | PC已验证 | 不增加引脚、线程或持久存储 | 未START完全跳过碎片视觉与规划；CAL仍实时分析；再次START重拍 |
| B版持久存储 | 保存透视展开版参数 | `/root/maixcam2_puzzle_B/vision_settings.json` | PC已验证 | 不适用 | 与稳定版、A版隔离 |

## 自动纸张 ROI 物理基准

| 项目 | 固定值 | 说明 |
|---|---:|---|
| 完整黑纸 | V=210×297mm；H=297×210mm | AUTO按两组对边平均像素长度判向，PAPER V/H可手动兜底 |
| A4毫米映射 | 四角Homography | 支持固定倾斜相机；要求纸面与碎片顶面近似共面，不替代镜头畸变校正 |
| 倾斜精度校验 | 100×60mm标准片中心加四角五点 | 各点长短边误差优先≤1.5mm，现场上限约2mm；超差时检查镜头畸变和高度视差 |
| 机械有效区 | V=(0,33.5,210,230)；H=(33.5,0,230,210)mm | X/Y/W/H以0.5mm步进调节，方向随LOCK ROI保存 |
| 区域边界 | 两轴均≤230mm且完整位于当前方向A4内 | 黄色框直接表示电机可达纸面 |
| 上下分界 | V默认148.5mm；H默认105mm | 两种方向都映射为水平红线，不依赖颜色检测 |
| 旧整体内缩 | 0～20mm | 仅用于A版V2迁移和B版v1.1.0兼容 |
| B版展开比例 | 2像素/mm | 完整A4为420×594，工作区为420×460 |

## 引脚、DMA、中断与时钟

当前视觉模块通过 MaixPy 高层接口访问板载设备，工程未直接配置 GPIO、DMA、中断优先级或芯片时钟，因此不填写未经代码证实的底层资源分配。

## 验证边界

| 类别 | 状态 | 证据或待办 |
|---|---|---|
| PC自动测试 | 已验证 | v1.10.0 A版定向177/177、硬门负例8/8；项目全量394/394通过 |
| Python编译与导入 | 已验证 | A版compileall退出0；A/B发布包独立目录导入通过 |
| MaixVision平铺发布 | 已验证 | A版v1.10.0、B版v1.1.0的文件清单、逐文件字节一致性和解压后`main.py`导入测试9/9通过 |
| MaixCAM2实机 | 未验证 | 需检查真实反光轮廓的SHAPE/TIER、三/四片耗时、目标角度、触摸响应和机械落点 |

## 变更记录

| 日期 | 变更内容 | 原因 |
|---|---|---|
| 2026-07-29 | 初始生成 | 记录当前视觉模块已确认的硬件约束 |
| 2026-07-29 | 增加A/B独立存储和A4物理基准 | 自动纸张ROI Round 1完成 |
| 2026-07-29 | 增加PC验收结果与实机验证边界 | 自动纸张ROI Round 4完成 |
| 2026-07-29 | A版计划改为可调X/Y/W/H/SPLIT机械区域 | 支持现场直接扩大、缩小和移动黄色区域 |
| 2026-07-29 | A版增加持久布局、稳定缓存和已知/未知拼装规划 | 发布v1.2.0并完成PC验收 |
| 2026-07-30 | A版改为1280×960分析、640×480显示，并增加MEASURE质量门 | 提升整张A4下2mm黑缝分辨能力 |
| 2026-07-30 | KNOWN改为下半区同步登记；UNKNOWN增加96宽有限边图和5秒工作单元边界截止 | 发布v1.4.0并限制跨帧搜索时间 |
| 2026-07-30 | UNKNOWN增加WHITE/CARD、32边/90组合`GRAPH_AUTO`、5秒累计计算/20秒墙钟双预算和倾斜五点校验 | 发布v1.4.1并提高现场三片白色碎片的求解速度与可验证性 |
| 2026-07-30 | A版增加横竖AUTO与PAPER V/H，KNOWN改为32点轮廓、3mm稳定容差和1.60阈值 | 发布v1.5.0并处理横放长宽互换及反光顶点抖动 |
| 2026-07-30 | A版增加单次碎片快照、超时保持、手动重拍和完整A4纸面专用显示 | 发布v1.6.0并消除坐标抖动、超时自动循环及纸外龙门架显示 |
| 2026-07-30 | A版增加92%/86%双层WHITE验收、可关闭求解日志和AUTO H/V状态 | 发布v1.7.0并提高噪声角点容错性与现场可诊断性 |
| 2026-07-30 | A版增加WHITE四片分层Beam、T形分段接缝、三角化重叠与8秒/30秒调度 | 发布v1.9.0并解决现场四片在第85节点被墙钟截断的问题 |
| 2026-07-31 | A版UNKNOWN改为完整轮廓＋最多3个接缝候选，三条路径共享刚体位姿和分级搜索 | 发布v1.10.0并修复反光伪短边造成的路径几何不一致与四片超时 |
