# 研究发现

## 2026-07-31 - AUTO ROI多阈值与旧ROI边缘恢复结论

- **trace_id**：`maixcam2-auto-roi-robust-recovery-20260731`
- **新增实机证据**：同一固定机位两次正确帧均为`best_area≈23.5% rect≈0.96 convex≈0.99`；误锁帧为`best_area=1.2%/largest_area=27.8% quad_eps=0.050`；最新失败帧为`best_area=34.3% rect=0.393 convex=0.481 dark=0.492`。前者证明宽松候选缺少主轮廓关系门，后者证明单次Otsu无法覆盖亮度不均或暗区粘连。
- **严格保护**：干净A4继续使用原Otsu、2%优先epsilon、0.70矩形度和0.65置信度；只有严格失败才扫描`Otsu±12/24`。严格四角若远小于同帧最大暗区也会安全拒绝，防止干净小矩形绕过宽松门。
- **联合安全门**：多阈值候选必须同时通过8%绝对面积、55%相对最大轮廓面积、0.35宽松矩形度、0.70 A4比例得分、0.42内部暗度、0.55四边平均支持和至少3条单边支持。不能单独降低矩形度或置信度。
- **边缘实现**：Canny容差边缘图每次AUTO只计算一次，最多供5个阈值、每阈值4个主轮廓复用；只沿已提出四边采样，不运行容易受龙门架干扰的全帧Hough。
- **固定机位兜底**：严格和多阈值均失败且已有旧蓝框时，只在旧四边法向4%画面短边范围内搜索当前梯度，由相邻修正线求交；候选仍须通过A4比例、暗度、边缘、0.55 IoU和总置信度，不能直接复用历史坐标。
- **诊断结论**：日志新增`source=STRICT/TH_SCAN/PRIOR_EDGE`、`used_threshold`、`area_to_largest`、`quad_fill`、`edge_support`、`edge_sides`、`prior_iou`及六类宽松拒绝门，可直接区分小候选、比例、暗度、缺边与旧ROI不一致。
- **验证结论**：24项新增/参数化行为均先红后绿；A版定位、入口与调参聚焦`139 passed`，项目测试`502 passed`，发布测试`10 passed`，A版compileall和`git diff --check`通过。
- **发布物**：`maixcam2_app_A_quad/dist/diansai_quad-v2.1.0.zip`为209242字节，SHA256 `E561E90BA0ED6B6EE37DE872B1B76B3402F816330D01C75BB6C8968AD53D0D20`。
- **实机边界**：PC合成图证明安全门和数据流，不代表真实MaixCAM2曝光、Canny支持率及单次AUTO耗时已经验收；部署后需提供新的完整`[ROI] AUTO`行，并核对蓝框四边而非只看confidence。

---

## 2026-07-31 - AUTO ROI 5/6角自适应四角结论

- **trace_id**：`maixcam2-auto-roi-adaptive-quad-20260731`
- **实机证据**：用户日志中最大暗色轮廓26.4%，无面积过大候选；44/49轮廓因面积过小过滤，剩余5个全部在四角门失败，其中3个严格近似为5角、2个为6角。矩形度和置信度尚未执行，降低两者无效。
- **根因**：旧`approxPolyDP`固定使用周长2%，纸边弯曲、反光或暗色粘连产生的额外凸点会令黑纸停留在5/6角并被直接拒绝。
- **实现结论**：保留2%严格路径，失败后依次尝试2.5%、3%、3.5%、4%、5%；第一个有效四角进入原有评分，面积、A4比例、0.70矩形度和0.65置信度均未放宽。
- **合成证据**：真实高斯模糊与Otsu后，5角和6角场景在2%失败、2.5%仍保留额外角、3%首次恢复400×280基础四角；干净纸继续使用2%。
- **安全边界**：配置要求非空、有限正数、严格递增且最大不超过10%；不使用`minAreaRect()`，避免侧装透视四角被强制为旋转矩形并污染Homography毫米坐标。
- **验证结论**：新增10项行为先按预期失败后转绿；定位器与入口`85 passed`，项目范围`400 passed`，A版compileall和`git diff --check`通过。

---

## 2026-07-31 - AUTO ROI失败门诊断结论

- **trace_id**：`maixcam2-auto-roi-diagnostics-20260731`
- **界面根因**：`CalibrationSession.apply_auto_roi()`把`no_candidate`和`low_confidence`统一显示成`AUTO ROI FAIL`，失败后保留旧蓝框；`UART:OFFLINE`只是独立心跳状态，因此仅看小屏幕无法确定本次AUTO在哪一道门失败。
- **硬门事实**：A版按暗色外轮廓依次执行1%～50%面积、四角拟合、0.70矩形度和0.65置信度门。侧装方向修复只作用于成功候选之后的H/V和坐标轴，没有修改这些筛选条件。
- **诊断实现**：每次AUTO返回轮廓总数、面积过小/过大、非四角、矩形度不足、有效候选、最大轮廓面积、非四角顶点分布和最佳候选评分；`main.py`在调试开启时只打印一行，定位器内部不逐轮廓输出。
- **合成证据**：约79.3%画面黑块输出`gates=AREA_LARGE`；细长暗块输出`gates=CONF_LOW`、`aspect=0.106`、`best_conf=53.7%`，可以直接区分面积门和置信度门。
- **验证结论**：新增3项用例先因缺少`diagnostics`按预期失败，实现后转绿；项目范围`390 passed`、A版compileall和`git diff --check`通过。
- **实机边界**：日志可以定位拒绝阶段，但真实画面是纸张超过50%、与龙门架/阴影粘连，还是透视比例过低，仍需用户点击AUTO后提供同一条`[ROI] AUTO`实机输出确认。

---

## 2026-07-31 - FOUR专用图实机快照根因与双核心结论

- **trace_id**：`maixcam2-a-four-dedicated-v210-20260731`
- **尺寸排除**：原生FOUR完整候选短边约90.3mm，临时增加2mm容差后仍全部低于85%填充；扩大到宽范围仍无解，因此尺寸只超0.3mm不是根因。
- **截断根因**：旧成功布局使用`U2→U1`分段、`U3→U1`整边和`U4→U3`分段。新关系全集包含正确`U2→U1`刚体位姿，但它排第31；当前每对6条以及24条实验都无法保留，扩大Beam到96同样失败。
- **排序根因**：原生图让首端、末端、中点三个锚点分别占名额，并把合法分段边的长度差作为高惩罚；旧实机快图按边对限宽、边长守恒和外框紧凑度排序，同一快照能以2条分段接缝得到严格矩形。
- **路由结论**：简化轮廓含五边形时优先实机快图，可从约2.18秒/3345节点降至约1.55秒/2401工作单元；全为三/四边形时优先原生图，保留规则四宫格和T形既有行为。两者共用3秒活动预算，不进入普通UNKNOWN通用FALLBACK或KNOWN模板。
- **机械位姿结论**：旧快图的目标多边形正确，但直接使用轮廓质心和最小外接矩形角度会产生约0.35mm中心偏差或180度二义性。由源/目标同序顶点做无镜像Kabsch后，发送中心和角度能逐点重建目标。
- **侧装坐标结论**：蓝框定位正确不代表纸面坐标轴正确。旧Homography始终把画面第一条边当X轴，侧装相机下红线、上下区、目标和UART毫米坐标会一起错转90度。布尔侧装值仍无法区分顺时针和逆时针，因此改为四值安装方向；AUTO先转换H/V，所有正反映射与FOUR展开复用同一逻辑四角顺序。
- **升级结论**：旧V5虽然保存了PAPER H/V，却没有保存侧装旋转正负。V6加载V5时必须保留分割参数但丢弃旧蓝框和工作区，否则升级后可能显示正确蓝框却继续使用反向毫米原点。
- **正常页显示根因**：侧装坐标修复后，显示Homography也复用了机械逻辑四角和210×297mm纵横比，导致CAL中的横纸在正常页被强制旋成竖纸。显示层应使用相机左上起四角和蓝框实际横竖比例；毫米映射、红线、目标与UART继续使用机械逻辑四角。FOUR调试掩膜则需要按两套四角的循环位移单独旋转。
- **审查结论**：FOUR_FAST在最后一个yield前写入合法计划但刚好跨过活动截止线时，必须克隆、尺寸复核并重建刚体位姿后返回；直接返回旧对象或一律报超时都不正确。FOUR复用UNKNOWN快图时还必须显式传入自己的尺寸、填充和重叠门，公共函数只通过旧默认值保持1～3片兼容。
- **UART结论**：实机四片计划编码为48字节RESULT载荷，解码为4条源中心、目标中心和有符号旋转角；协议模式继续为UNKNOWN，F4解析无需新增模式码。
- **验证边界**：PC全量`387 passed`并证明算法、调度、两种侧装方向、横向正常页、V5迁移和协议数据自洽；真实MaixCAM2耗时、光照阈值、侧装方向正负、UART4电气与F4机械动作仍需上板验证。

---

## 2026-07-31 - v2.0.1审查修复结论

- **trace_id**：`maixcam2-a-uart4-review-fix-20260731`
- **横放结论**：完整横纸区域必须把`paper_orientation=landscape`传到运行上下文、目标区域校验、KNOWN登记和SAVE缓存；仅在显示层交换长宽不足以防止297mm宽度被竖纸210mm上限拒绝。
- **状态结论**：SEND A4的ACK/NACK发生在点击后的后续`poll()`帧，CAL绘制必须读取通信运行器最近事件；成功规划的NaN、字段缺失或定点溢出必须在协议边界转为`RESULT ERROR`，不能穿透相机主循环。
- **会话结论**：仅用`TYPE+SEQ`无法跨Maix重启安全去重。10字节心跳增加随机非零`session_id`后，F4以会话为去重命名空间；首次合法心跳前禁止业务，变化时清空旧去重和未执行旧方案。由于该心跳与旧6字节格式不兼容，通用帧版本必须同步提升为`VERSION=0x02`，不能继续冒充协议版本1。
- **UART4占用结论**：不再依赖UART构造函数隐式释放默认监听。应用明确执行`rm_default_comm_listener()`后打开`/dev/ttyS4`，打开失败或租约关闭时执行`add_default_comm_listener()`恢复系统资源。
- **状态码结论**：心跳统一为0待机、1调参、2采集、3求解、4结果就绪；F4可据此屏蔽旧方案和调参期间机械动作，但机械急停与软限位仍由F4独立负责。
- **验证结论**：项目全量420项、UART协议33项、发布9项通过；compileall和diff检查通过。v2.0.1 ZIP为160011字节，SHA256 `FF7A9E5BC94F2C2A6A8EB03415DC70B0D45173D4DC183DEAB7F3EC4FAC39A172`，14个文件与源码逐字节一致。
- **未验证边界**：上述证据来自PC替身和静态发布检查；真实MaixPy `maix.comm` API、UART4电气、F4解析/ACK、跨重启清缓存和机械动作必须上板验证。

---

## 2026-07-31 - v2.0.0发布审查根因

- **trace_id**：`maixcam2-a-uart4-review-fix-20260731`
- **横放崩溃根因**：`main.py`把横放完整区域`(0,0,297,210)`传入`AssemblyRuntime.update()`，但`_build_context_key()`和目标矩形辅助函数仍调用默认竖放的`validate_work_region_mm()`，因此开始识别后抛出`work_width_mm必须位于0到210之间`，异常未在主循环隔离。
- **CAL反馈根因**：点击`SEND A4`时只把当时的`A4 QUEUED`复制到`CalibrationSession.status_text`；后续`poll()`产生的`A4 ACK`、`A4 NACK n`或写入错误没有同步回CAL状态栏，心跳ACK仍可能令链路显示`UART:OK`。
- **结果异常根因**：成功规划的串口编码发生字段缺失、NaN或定点溢出时，异常会从`queue_successful_plan_result()`直接穿透`run_app()`，导致视觉主循环退出。
- **去重根因**：视觉程序每次启动都从SEQ=0开始，F4仅按`TYPE+SEQ`去重时无法区分新启动会话；Maix单独重启后，新结果可能与F4缓存中的旧结果冲突。
- **UART4占用风险**：用户实机日志明确显示Maix Comm Protocol已经把A21/A22映射为UART4并监听`/dev/ttyS4`。现有设计文档声称应用打开同一UART会自动释放该监听，但当前官方网页直链查询未取得支持证据，其中两个页面返回404；此结论在找到官方源码或实机证据前标记为未验证，不得作为发布保证。

---

## 2026-07-31 - UART4协议、毫米坐标和完整A4区域结论

- **trace_id**：`maixcam2-a-uart4-protocol-20260731`
- **坐标结论**：调试帧和拼图结果全部使用完整A4左上角原点的毫米坐标，不发送相机像素。PAPER_FRAME因此是当前V/H方向的规范四角：竖放210×297mm、横放297×210mm；它用于确认坐标系和方向，不携带相机透视四角。
- **结果结论**：PUZZLE_RESULT一帧包含1～4片全部源中心、目标中心和旋转增量；坐标和角度均乘10为int16小端。纸面Y轴向下，因此当前规划正角从纸面上方观察为顺时针。
- **可靠性结论**：500ms心跳和1500msACK在线门只判断链路；PAPER/RESULT使用CRC16-CCITT-FALSE、相同TYPE+SEQ重发和F4去重。F4收到重复帧必须只重发ACK，不能重复执行电机动作。
- **实时性结论**：UART构造延迟到主循环poll，读写异常转为`UART:ERROR/OFFLINE`；每帧只做一次非阻塞read和有限短写，不改变相机、触摸、稳定门或v1.9.2求解器。
- **区域结论**：A版黄色视觉区域可等于完整蓝框，校验上限改为当前A4宽高；AUTO成功无条件重置完整区域、中线和INSET。230×230mm是机械端约束，F4仍须根据零点、停车位和轴方向做软限位。
- **兼容结论**：A版增加第六槽，B版继续五槽；A版V2设置仍迁移到历史210×230mm区域，避免旧设备升级后保存区域静默跳变；B版发布包哈希不变。
- **验证结论**：协议27项、调参与设置60项、入口通信90项、项目范围406项、发布9项和compileall均通过。A版v2.0.0 ZIP为156027字节，SHA256 `96B79CCD5A8221429CD421608F16CF6143B14D1EE148806B0B0F0E7E02A1008C`。
- **未验证边界**：PC不能证明MaixCAM2 A21/A22电气连接、`/dev/ttyS4`设备可用性、F4 DMA/中断状态机、实际ACK延迟、机械动作去重和软限位；必须按协议文档用逻辑分析仪和禁用电机输出的方式分阶段联调。

---

## 2026-07-30 - v1.9.2四片FOURFAST耗尽统一预算根因与修复结论

- **trace_id**：`maixcam2-a-four-fast-active-budget-v192-20260730`
- **现场根因**：四片日志在GRAPH 520ms失败后进入FOURFAST，最终29.315秒墙钟返回`source=FOUR_FAST reason=solver_timeout`；FOURFAST占满统一8秒活动预算，使原FALLBACK根本没有启动机会。
- **离线对照**：同一毫米顶点在Beam32下检查2400单元仍无解，提高工作上限至10000仍无解；Beam96在5220单元成功，但扩大Beam只会继续拖慢设备。原FALLBACK在第229节点即可得到`fill=93.3% overlap=1.1%`的合法解，说明正确修复是限制快路径占用，而不是继续扩大快路径。
- **预算结论**：FOURFAST使用1.5秒独立CPU活动预算，只在恰好四片WHITE且`current_stage=four_fast`时累计。到期是阶段无解，不是UNKNOWN超时；下一帧必须继续FALLBACK并保留统一8秒预算的剩余部分。
- **三片结论**：三片从不进入FOURFAST。用户三片日志的`GRAPH → FALLBACK`首解第43节点、活动1.841秒、墙钟6.141秒路径和全部全局调度参数保持不变。
- **诊断结论**：`limit=1`只表示2400候选工作量上限，`time_limit=1`只表示1.5秒活动上限，`active_ms`表示FOURFAST自身CPU耗时；只有最终`RESULT reason=solver_timeout`才是统一任务超时。
- **最终证据**：专项114项、项目范围364项、发布9项全部通过，compileall退出0。A版v1.9.2 ZIP为137649字节，SHA256 `E5E57AE830CFF2EE6751AA44FC456935DA6D519F8495CF6674429B09384A15D4`；B版固定哈希不变。

---

## 2026-07-30 - v1.9.1四片实时性复审根因与修复结论

- **trace_id**：`maixcam2-a-four-piece-fast-solver-v191-review-20260730`
- **早期误剪根因**：快速重叠原先只用当前已放片与候选面积作分母；两片阶段的1.6mm浅交叠可被放大到4%以上，即使加入剩余碎片后的最终矩形能通过3%硬门，也会在搜索早期丢失。
- **面积口径结论**：求解开始时计算最终全部碎片总面积，当前累计交叠从该总面积中扣除后估算最终联合面积。凸多边形、凹多边形三角化和异常MASK回退必须使用同一口径；最终栅格3%机械硬门保持不变。
- **截止线丢解根因**：真实GRAPH和FOUR_FAST形成合法规划后先yield，下一次恢复才把结果返回流水线；若该工作单元刚好越过活动预算，外层会在yield后关闭生成器，尚未写入共享`best_plan`。
- **截止线结论**：两个快路径都在yield前保存通过硬门的独立规划快照。真实GRAPH慢验收推进假时钟5.1秒的回归已证明超时收尾返回成功并带`returned_best_at_timeout=1`。
- **诊断根因**：旧`parents`在循环前直接写保留状态数，默认2400上限实际只展开24个完整层父状态却显示32；GRAPH失败事件后同一帧继续执行FOUR_FAST，还会使GRAPH日志耗时混入下一阶段。
- **诊断结论**：父状态实际进入展开循环时才计数；阶段失败事件入队后强制让出，`UnknownSolveJob.advance()`见到新事件立即结束本帧；日志新增`limit`。现场快照默认`parents=1/32/24 units=2400 limit=1`仍能从完整候选成功，不应误判为故障。
- **最终证据**：专项107项、项目范围357项、发布9项全部通过，compileall退出0。A版v1.9.1 ZIP为136221字节，SHA256 `CAD7F8619FB9FD2F2FF497D2536F6D587D4D8DE6B2961868DADC0918E85351AB`；B版固定哈希不变。

---

## 2026-07-30 - 四片WHITE超时、分段接缝与搜索成本根因

- **trace_id**：`maixcam2-a-four-piece-fast-solver-20260730`
- **识别排除**：现场日志已稳定锁定4片，毫米边长与最终可解布局一致；同一输入旧FALLBACK能得到填充95.6%、重叠1.5%的严格结果，因此失败不在ROI、Homography或92%填充门。
- **GRAPH边界**：把32边/90组限制扩大到全部32条整边候选和1016组连通关系仍无解。正确布局需要短边贴合长边局部区间，整边GRAPH无法表示T形`segmented`接缝。
- **超时根因**：设备在20.534秒墙钟内只获得3.319秒求解活动时间并推进85节点；PC旧FALLBACK首解位于第168节点。单纯增大Beam到192/384不改变首解节点，延长超时只能掩盖搜索与调度成本。
- **分层方案**：FOUR_FAST固定第0片，复用现有整边/分段兼容图，按两片、三片、四片逐层全局去重排序；每个有向片对只展开前6条关系，每层默认保留32状态并只扩展前8个父状态，达到2400工作单元自动转旧FALLBACK。
- **重叠结论**：简单凹五边形若每次创建毫米MASK仍是主要热点；耳切为三角形后可用`intersectConvexConvex`精确累计。中间候选以单片面积为分母，需用4%预筛吸收正确U2约3.23%的局部交叠，最终仍按四片联合面积执行3%硬门。
- **结果证据**：现场快照FOUR_FAST使用708工作单元和2条分段关系成功，`fill=95.6%`、`overlap=1.5%`、`outer=4`；PC热运行约108～116ms。该时间不等同于MaixCAM2，设备日志是最终性能依据。
- **调度结论**：旧FALLBACK保护调整为8秒活动时间/30秒墙钟，每帧24ms/64工作单元；WHITE兜底可使用快速几何重叠，CARD继续保留原MASK与纹理择优语义。
- **最终证据**：定向70项、项目范围348项、发布9项通过，compileall退出0；A版v1.9.0 ZIP为130892字节，SHA256 `4466775E4E7E5A3AF003DAE0F6D39C2720C08610F97A267A300FFD306F4B312A`；B版固定哈希不变。

---

## 2026-07-30 - WHITE伪短边与正常页自动分析根因

- **trace_id**：`maixcam2-a-solver-clean-start-gate-20260730`
- **伪短边根因**：实机毫米轮廓中的2.3、6.7和10.4mm短边来自白色覆盖不完整或反光小切角；全局增大`approxPolyDP`会同时威胁题目真实五边形角，清理应限定在WHITE求解副本。
- **删除规则**：对最短伪边的两个端点分别计算“删点后该点到新弦线的距离”，删除几何变形更小的一端；每次重新计算循环边，至少保留3点，面积退化则拒绝本次删除。
- **重要回归结论**：让WHITE兜底也使用清理轮廓会使旧88.8%容错用例从成功变为`fill_reject`，因为短边端点仍携带有效面积。因此只有GRAPH快路径清理，兜底恢复原始锁定顶点；CARD保留原顶点和纹理。
- **START根因**：旧`activate_capture_mode()`把模式选择与正式采集绑在一起，`should_analyze_live_frame()`只检查CAL和快照缓存，导致程序启动或切换模式后立即分割、累计稳定帧和求解。
- **状态机结论**：模式/材料只选择并取消活动采集，START才重置并置`capture_armed=True`；CAL无条件实时分析，正常页未START时只构造零掩膜显示数据，不进入视觉、模板或规划路径。
- **SAVE门结论**：KNOWN未START时在重置运行器和模板登记前返回`PRESS START`；已START才清旧状态并沿用固定规模同步登记。
- **最终证据**：求解清理与START定向101项、项目范围339项、发布9项通过；compileall退出0。A版v1.8.0 ZIP为123753字节，SHA256 `39001CE38B657806FF6CB05DB49A482D6A010E59ACC667DF29C936B7D69856F2`；B版固定哈希不变。

---

## 2026-07-30 - 三片随机碎片超时与横纸AUTO复查

- **trace_id**：`maixcam2-a-noisy-corner-landscape-debug-20260730`
- **实机分层证据**：用户截图为`UNKNOWN N=3 EDGE=0 ... LOCKED PLAN SOLVER...`，三片均有完整绿色轮廓并已锁定，因此本次失败发生在毫米顶点进入拼装器之后，不是碎片数量、触边或重复换帧问题。
- **截图轮廓复现**：从正常页红色角点近似还原5边形、4边形和3边形，以纸面显示比例扫描合理毫米尺度后，现有WHITE图路径均无解；兜底搜索能形成尺寸约`89.10×56.05mm`、重叠约`0.59%`且三片均有目标外边的组合，但最佳填充率约`88.78%`，低于生产硬门`92%`，最终报告`fill_reject`。
- **门槛探针**：同一近似轮廓在填充门`92%`和`90%`时失败；试验性设为`88%`时在第40节点成功，PC总耗时约24ms。该探针只证明硬门是可复现拒绝点，尚不能直接把全局门槛改成88%，因为必须增加“每片至少一条目标外边”和最佳候选评分来控制误解风险。
- **快路径缺口**：A版GRAPH只枚举近似等长的整边连接并用单一中点锚定，实际5+4+3分片很可能包含长边由两段覆盖的T形接缝；图路径因此失败后进入较慢兜底。K230参考程序会对全部图布局择优，验收比A版92%填充硬门宽松；A版此前只移植了32边/90组合结构，没有移植其宽容评分策略。
- **角点机制风险**：A版`approximate_polygon()`从小epsilon开始，一旦首次得到3～5点就立即返回；白纸覆盖不足或反光形成的小切角可能保留为额外顶点并切断真实长接缝。直接增大epsilon会误删真实五边形角，因此更稳妥的修复位置是求解器的容错候选与二级验收，而不是全局粗暴减少顶点。
- **横纸截图判断**：CAL截图底部显示`X 33.5mm`，这正是landscape默认机械区`(33.5,0,230,210)`，蓝框左右内缩后的黄色框和105mm红色中线也与横纸逻辑一致；当前截图本身已经被判为H。现有缺陷是AUTO状态只写`AUTO ROI OK`，未直接显示H/V，现场无法直观看出判向结果；仍需增加截图式强透视横纸回归和明确方向诊断。
- **调试需求**：增加默认可关闭的串口控制台开关，开启时打印锁定毫米顶点/边长、GRAPH候选/组合/最佳指标、兜底拒绝计数、节点与耗时、AUTO四边长度/方向；关闭时不格式化、不打印，避免影响比赛速度。
- **实现结论**：WHITE采用92%严格优先、86%容错兜底；容错层保持尺寸范围和3%重叠门，并强制每片至少一条目标矩形外边。实机近似5+4+3轮廓由GRAPH有界路径成功返回，CARD同一输入仍因严格填充不足而拒绝，内部片负例返回`outer_edge_reject`。
- **搜索结论**：GRAPH最多90组内发现严格解立即结束；只有没有严格解时才返回评分最低的容错候选。分段边兜底发现容错首解后再比较64节点，避免候选顺序决定结果，同时不再跑满12000节点。
- **日志结论**：默认开关开启时只在AUTO、快照锁定、每片几何、GRAPH完成、兜底结束和最终RESULT输出一次；关闭测试确认没有任何`[SOLVER]`或`[ROI]`输出。超时结果会携带当时累计的尺寸、填充、重叠和外边拒绝数。
- **横纸结论**：AUTO状态改为`AUTO ROI OK H/V xx%`。强倾斜横纸测试继续得到H、`(33.5,0,230,210)`机械区和105mm分界线；Homography链未改动。
- **最终证据**：定向139项、项目范围320项、发布9项全部通过；compileall退出0。A版v1.7.0 ZIP为119891字节，SHA256 `C191FCE2D73F778AB9829C8151B60DDF9A4D06F14036E70D49F8CC562E1D2576`；B版固定哈希不变。

---

## 2026-07-30 - UNKNOWN单次快照与A4纸面显示根因

- **trace_id**：`maixcam2-a-snapshot-paper-view-20260730`
- **快照现状**：`AssemblyRuntime.update()`在UNKNOWN任务存在时确实忽略后续实时`pieces`，单次任务已经使用第3稳定帧；屏幕红点继续变化来自`run_app()`始终把当前帧`pieces`传给`draw_overlay()`，不是求解器正在换坐标。
- **循环根因**：`_advance_unknown_job()`遇到无首解`solver_timeout`会清空`plan`、观测和稳定计数，后三帧重新创建任务，因此现场会看到超时、重新识别和再次搜索的循环。
- **检测边界**：`analyze_quad_frame()`把黄色机械四边形传给`detect_pieces()`；`build_foreground_mask()`在形态学与拓扑填孔之后两次应用四边形掩膜，所以纸外像素已经不能进入轮廓。黄色区域是完整A4内部比纸面更严格的可达边界。
- **显示根因**：现有`draw_overlay()`只是把整幅1280×960相机画面缩到640×480，纸张四角仅作为叠加线，因此纸外龙门架和地面仍可见。
- **确认方案**：连续3帧后深复制一次快照，求解成功或失败均保持锁定；同模式按钮、WHITE/CARD切换或CAL才重新采集。正常界面用仅显示用途的Homography等比例呈现完整A4，识别和机械毫米映射保持原链路。
- **实现结论**：`AssemblyRuntime`在第三个稳定帧创建不可变碎片快照；`run_app()`在`LOCKED`期间直接复用快照轮廓绘制，跳过正常页的重复分割和轮廓提取。KNOWN保存成功也同步建立同类快照，解决“规划使用旧坐标、屏幕红点仍跟随新帧抖动”的视觉错觉。
- **失败保持**：无首解超时不再清空观测并自动重启，界面保持`LOCKED PLAN SOLVER_TIMEOUT`。用户再次点击当前模式、切换WHITE/CARD或进入CAL会显式清除锁定状态，因此现场重拍由用户决定。
- **纸面显示实现**：显示链先用`order_a4_quad()`统一四角循环顺序，再将完整A4透视展开到按210:297或297:210等比例计算的视口；视口以外填黑。该变换只服务显示，识别仍在原相机高分辨率坐标中执行，避免显示缩放改变毫米精度。
- **检测范围结论**：完整A4四角负责排除所有纸外相机内容；实际碎片检测继续使用位于A4内部的黄色机械有效四边形作为更严格掩膜。这样既满足“只检测标定纸面”，又不会把电机不可达的上下/左右舍弃区误当作可抓取碎片。
- **回归修复**：独立审查发现任意循环起点或反向输入的A4四角会令显示变换错位，以及KNOWN SAVE成功后未建立快照；两处均已修复，并新增四角换序零像素误差与KNOWN SAVE锁定测试。
- **最终证据**：核心定向`75 passed in 0.91s`，项目范围`310 passed in 3.28s`，compileall退出0，发布测试`9 passed in 0.64s`；A版ZIP内12个发布文件与当前源码逐字节一致。

---

## 2026-07-30 - 横放长宽互换与KNOWN顶点抖动根因

- **trace_id**：`maixcam2-a-orientation-known-robustness-20260730`，RESEARCH / IMPLEMENT / REVIEW。
- **横放根因**：旧`order_a4_quad()`只保证屏幕左上、右上、右下、左下顺序，后续Homography无条件映射210×297mm；横纸的屏幕水平长边因此被错误当成210mm，导致毫米长宽整体互换。
- **方向结论**：纸张方向必须是锁定ROI的一部分，而不能在规划器内猜。AUTO用两组对边均值判向以减小透视远边缩短影响；手动PAPER V/H是现场遮挡或极端透视的确定性兜底。
- **机械区域结论**：230×230mm行程与A4相交后，竖放默认210×230mm并上下裁33.5mm，横放默认230×210mm并左右裁33.5mm；红线继续按纸面Y坐标水平分区。
- **KNOWN根因**：旧描述子对顶点数差加入整数惩罚，稳定门在4/5顶点不等时直接False，姿态函数也要求等顶点数。PC探针中2mm毛刺从4点变5点后旧分数约3.50并失败，单纯把阈值提高到4.0会增加K编号错配风险。
- **稳健方案**：沿闭合轮廓等弧长采样32点；形状匹配中心化并归一尺度，姿态保留毫米尺度，稳定门保留绝对位置；只枚举循环起点和正旋转，不反转遍历，因此镜像形状不能借由遍历反向通过。
- **阈值结论**：重采样解决离散顶点根因后，阈值只需适度调整为1.60；中心稳定容差调整为3mm且继续要求连续3帧。2mm毛刺、4/5顶点和旋转姿态测试通过，镜像反例仍高于阈值。
- **性能结论**：二维正旋转用复数相关的单位相位闭式求解，与2×2 Kabsch等价；消除每个循环起点的SVD后，四片纯匹配PC平均约7.825ms。
- **最终证据**：项目范围`295 passed in 3.26s`、compileall退出0、发布测试`7 passed in 0.55s`；稳定版9项哈希一致，B版ZIP哈希不变。

---

## 2026-07-30 - K230 GRAPH_AUTO移植与倾斜Homography验证

- **trace_id**：`maixcam2-a-graph-fast-path-20260730`，IMPLEMENT / REVIEW。
- **参考程序结论**：K230参考实现的速度来自先建立少量边连接、枚举连通关系并一次传播全部位姿，而不是放宽最终矩形条件。原样照搬其评分会缺少A版真实毫米尺寸、填充率和重叠率约束，因此只移植连接图结构。
- **图路径实现**：WHITE最多保留32条跨碎片整边候选，最多枚举90组边不复用、度数受限且覆盖全部碎片的连通关系；从第0片开始BFS传播旋转和平移，闭环计算误差，最终统一进入`_canonicalize_complete_layout()`硬验收。
- **回退边界**：图路径成功立即返回且不创建`UnknownSolveJob`；图无解时继续原96宽增量搜索，复杂T形分段接缝仍可处理；CARD为保留花纹比较而完全跳过WHITE图路径。一次性生成器输入在入口物化，避免图路径消费后兜底误见空集合。
- **鲁棒性证据**：明显倾斜的两组A4四边形经生产Homography反算后得到同一毫米布局；现场近似三片在1.5mm确定性顶点误差下走`graph_fast_path=1`；非矩形三角形不能绕过硬验收；候选边和连接组分别不超过32和90。
- **PC性能探针**：三片在1.0、1.5、2.0mm确定性顶点误差下均能走图路径；热运行约1.6～2.7ms，通常使用9～11条边、18～28组连接、2～6个完整布局验收。该数字只代表PC合成输入，不等同于MaixCAM2实机耗时。
- **Homography结论**：当前链路为像素顶点→A4四角逆Homography→纸面毫米→GRAPH_AUTO→下半区毫米目标→正Homography绘屏，支持固定倾斜相机。Homography不能修复镜头径向畸变或碎片高度视差，现场必须用真实100×60mm标准片在中心和四角做五点RECT校验；任一点误差超过约2mm时优先做相机内参/畸变校正或减小倾角。
- **最终证据**：项目范围`278 passed in 3.26s`、compileall退出0、发布测试`7 passed in 0.62s`；稳定版9项哈希一致，B版ZIP哈希不变。

---

## 2026-07-30 - 三片MASK正确但UNKNOWN超时

- **trace_id**：`maixcam2-a-white-card-anytime-20260730`，RESEARCH / PLAN。
- **实机证据**：MASK页显示三个互不连接的完整白色轮廓，`N 3/4 EDGE 0 SMALL 0 LARGE 0`；正常UNKNOWN页为`N=3 EDGE=0`后返回`PLAN SOLVER_TIMEOUT`，可排除粘连、漏检、面积过滤和黄色区域触边是本次主要原因。
- **控制根因**：`UnknownSolveJob`从创建时刻计算5秒总墙钟，而每帧只允许12ms/32工作单元；相机采集、显示和帧间等待会消耗墙钟但不会推进搜索。截止处理直接关闭生成器，只返回失败，没有读取搜索核心已经找到的`best_candidate`。
- **白片误判风险**：旧核心用任意一条边的`pattern_energy>=0.05`认定存在牌面纹理；镀锌铁片反光可能让白片进入400节点纹理择优，延迟本可立即返回的首个矩形。
- **覆盖缺口**：既有1～4片参数化测试中的三片是规则竖条，未单独覆盖两块四边形加一块三角形的现场形状。新增近似现场分割的PC探针成功，结果为2个搜索节点、34个边候选、最大前沿13，证明清洁三片几何可被核心表示，实机问题集中在跨帧控制与现场毫米/顶点数据。
- **决策**：显式WHITE/CARD取代自动纹理类型判断；WHITE首解即停，CARD保留有限纹理择优；累计计算预算与20秒硬墙钟分离，截止时优先返回当前最优成功规划。

---

## 2026-07-30 - A版v1.4.0 UNKNOWN有限边图与截止线验证

- **trace_id**：`maixcam2-a-stable-fast-vision-20260730`，Round 4 / REVIEW。
- **旧热点证据**：复杂带纹理四片PC为104.985ms；`_candidate_contact_length()`累计扫描全部候选与已放置多边形边对。临时令该函数返回0后同输入降到55.571ms，且相关正确性测试仍通过，证明它只是昂贵排序项而非硬验收条件。
- **边图决策**：每片边长只计算一次，求解开始时建立最多192个四片四边形有向边对；关系分为等长`full`与满足长度守恒的`segmented`，并缓存纹理分数、匹配长度和长度误差。
- **限宽决策**：按部分布局紧凑度、整边优先、纹理连续性和匹配长度排序，每层最多保留96个状态；最终尺寸、填充率和重叠率仍由完整布局栅格硬验收。
- **T形边界**：110mm长边由31/37/42mm三段覆盖的测试和约1mm顶点噪声测试均成功，说明预计算边图没有误删题目允许的分段接缝。
- **纹理边界**：整边反向重合可安全预计算纹理分数；T形分段边的两个端点锚定对应长边不同子区间，当前边图不携带子段特征，因此分段关系只按几何排序，避免伪纹理分数误导96宽前沿。
- **截止线**：`UnknownSolveJob`从创建时刻使用单调时钟计时，在工作单元前、生成器完成/异常后及时间片末尾检查5秒截止线，并返回不含机械目标的`solver_timeout`；新增单工作单元从4.9秒跨到5.1秒的回归用例完成RED→GREEN。单个正在执行的底层OpenCV调用不能被协作式生成器抢占，每帧12ms/32工作单元预算继续保留。
- **性能结果**：最终复杂带纹理四片五次PC为54.581/55.532/56.962ms（最小/中位/最大），搜索83节点、边候选134、最大前沿59；T形14.274ms、约1mm噪声T形17.210ms、规则四宫格13.351ms，均低于96前沿上限。
- **最终证据**：新增截止线片尾边界用例完成RED→GREEN；UNKNOWN/运行器定向43项、项目范围260项、compileall、发布测试、稳定版9项哈希和B包固定哈希全部通过。
- **实机边界**：PC性能不能直接换算MaixCAM2耗时；1280×960通道、首次边图预计算单元耗时、实际帧率和真实牌面成功率必须在设备上验证。

---

## 2026-07-30 - 实机两分钟SAVE、2mm粘连与PnP判断

- **trace_id**：`maixcam2-a-stable-fast-vision-20260730`。
- **截图证据**：`KNOWN N=4 EDGE=0`表示四片均为完整可操作轮廓；`SAVE SOLVING N=644`表示SAVE任务已经冻结当前四片并实际推进，后续帧识别抖动不会重启该任务。
- **速度根因**：`KnownSaveController.start()`创建`KnownRegistrationJob`，后者仍调用UNKNOWN逐片候选搜索；节点N只在候选集合排序后深入状态时增加，单个节点之间包含大量重叠栅格和接触长度计算，因此MaixCAM2两分钟仍不能完成。
- **分割根因**：A版固定采集640x480，截图中A4宽边约180～200原始像素，约0.9px/mm，2mm黑缝只有约1.8px；默认5x5高斯和5x5闭运算足以跨过该缝隙。粘连发生在像素分割阶段，早于毫米反算。
- **官方API证据**：Sipeed MaixPy `maix.camera.Camera`文档写明MaixCAM2最高支持2560x1440，并提供`add_channel()`、`set_resolution()`和`set_windowing()`；因此可采用高分辨率识别和640x480显示分离，但具体1280x960帧率需实机验证。
- **毫米映射判断**：当前210x297mm四角单应方法适用于固定共面纸片；四角会被定义性地映射为A4尺寸，不能证明中间区域精度。应使用真实100x60mm标准片在五个位置验证，并在边缘误差显著时增加镜头畸变标定。
- **PnP边界**：PnP不能分割粘连轮廓、不能求拼图组合、不能增加像素密度；只有需要相机三维姿态、Z高度或手眼标定时才有价值。二维龙门架继续使用畸变校正后的平面单应。
- **外部算法基准**：网上`puzzle_sim.py`原样对当前四片输入在PC需5.78到12.61秒，原因是最多80边候选后的组合枚举；只移植边候选、邻接图和全局评分思想，不能照搬穷举实现。
- **KNOWN流程结论**：当前代码只筛选`region=upper`，所以下半区正确布局无法保存。新流程改为下半区100x60mm正确布局直接登记，正式运行时再对上半区随机位姿做模板匹配。

---

## 2026-07-30 - A版v1.3.0冻结根因与v1.3.1修复

- **trace_id**：`maixcam2-a-realtime-solver-20260730`，RESEARCH。
- **实机复现路径**：UNKNOWN在稳定计数从`2/3`进入第3帧时调用未知求解器；KNOWN点击SAVE后经布局登记函数调用同一个未知求解器，两者均位于相机显示主循环线程。
- **直接根因**：真实牌面边缘的`pattern_energy >= 0.05`会关闭白片首解早停。求解器即使已经找到合法矩形，仍为优化纹理分数继续搜索至`max_nodes=12000`，期间显示与触摸均无法刷新。
- **PC计时证据**：4片带纹理斜四边形跑满12000节点耗时14.17s；累计热点为接触边排序6.65s、候选重叠栅格化3.84s、完整布局验收1.77s。MaixCAM2实机算力下冻结时间只会更长。
- **预算对比**：同一输入50节点已得到合法矩形且耗时0.088s；100/400/1200节点分别为0.113s/0.327s/0.877s，12000节点为12.17s。现有节点上限只限制搜索规模，不限制一次主循环的墙钟时间。
- **方案决策**：采用跨帧增量求解；不使用后台线程。每次推进同时受12ms墙钟预算和32个高成本工作单元限制，模式切换或CAL会取消旧任务。
- **纹理停止规则**：带纹理找到首个合法矩形后只继续400个搜索节点，不再为全局纹理最低分跑满12000节点；无解输入仍保留总节点上限。
- **状态数据流**：UNKNOWN显示`SOLVING N=...`；KNOWN SAVE显示`SAVE SOLVING N=...`，只有成功生成模板和规划后才原子写文件并显示目标。
- **异常边界**：模板指纹、KNOWN匹配和增量推进异常均转成`TEMPLATE ERROR`、`PLAN SOLVER_ERROR`或`SAVE ERROR`；SAVE先缓存校验再原子写盘，任一步失败保留旧磁盘模板。
- **修复后性能**：复杂UNKNOWN输入为405节点、39时间片，PC单次推进最大12.10ms；复杂KNOWN SAVE为432节点、46时间片，单次推进最大12.04ms。
- **PC验证**：关键与发布路径47项、项目范围229项、compileall、稳定版9项哈希与B包哈希全部通过。
- **实机边界**：PC时间片证明控制逻辑生效，但MaixCAM2单个OpenCV栅格工作单元耗时和最终帧率仍需部署v1.3.1测量。

---

## 2026-07-29 - A版v1.2.0最终PC审查

- **trace_id**：`maixcam2-a-assembly-20260729`，Round 4 / REVIEW。
- **缓存行为**：相同模式、模板和机械参数下连续3帧稳定才求解；成功或失败规划均缓存，碎片开始移动不会重复搜索。
- **持久路径**：视觉设置为 `/root/maixcam2_puzzle_A/vision_settings.json`，已知形状与布局为 `/root/maixcam2_puzzle_A/known_templates.json`，均不依赖 `/tmp/maixpy_run`。
- **鲁棒性修复**：新布局字段损坏不会再把异常泄漏到主循环，返回`template_layout_invalid`且不携带目标。
- **白片性能**：四个50×30mm测试片从12000节点/2.67s优化为19节点/约0.01s；有牌面纹理时继续比较同几何候选。
- **最终证据**：201项测试、compileall、7项发布测试、三套源码导入、稳定版9项哈希全部通过；B包哈希不变。
- **验证边界**：上述均为PC与合成图证据，MaixCAM2实际算力、真实牌面接缝、相机曝光和机械落点仍未验证。

---

## 2026-07-29 - 未知拼图回溯与牌面接缝验证

- **trace_id**：`maixcam2-a-assembly-20260729`，Round 3。
- **几何搜索**：固定第一片消除全局自由度，只枚举长度误差在容差内的反向边刚体对齐；每个候选先用内缩毫米栅格排除真实重叠。
- **完整验收**：组合旋正后要求长边88～122mm、短边48～92mm、填充率不低于0.92、重叠率不高于0.03。
- **牌面评分**：从每条像素边内侧2px和4px采样BGR与切向梯度；纯白能量低于0.05时纹理项为零，有图案时反向接缝越连续分数越低。
- **停止保证**：`max_nodes`为硬上限，无解返回`no_assembly/search_limit`且不携带机械目标。
- **PC证据**：红灯11项；实现后定向11项、全量194项通过。
- **实机边界**：牌面反光、像素采样距离和MaixCAM2实际求解耗时仍需现场验证。

---

## 2026-07-29 - 已知四片布局与毫米数据流验证

- **trace_id**：`maixcam2-a-assembly-20260729`，Round 2。
- **毫米数据**：单帧为每片保留像素轮廓，同时附加批量单应反算的 `vertices_mm`、`center_mm` 和按全部顶点判定的区域字段。
- **布局登记**：KNOWN SAVE要求四片完整且都在下半区；正确布局整体归一到100×60mm并与稳定K1～K4描述子绑定。
- **快速规划**：运行阶段复用最多24种一对一模板分配，再用行列式为正的二维刚体配准输出旋转增量，不允许镜像。
- **PC证据**：Round 2两次红灯共11项预期失败；定向16项、全量183项通过。
- **实机注意**：保存正确布局时相邻片保留约1～2mm黑缝，否则同色白片完全接触会成为单个连通轮廓。

---

## 2026-07-29 - A版设置V3与机械区域验证

- **trace_id**：`maixcam2-a-assembly-20260729`，Round 1。
- **迁移结果**：V2 `INSET=i` 映射为 `X=i、Y=33.5+i、W=210-2i、H=230-2i`，因此升级前后黄色区域不跳变。
- **交互结果**：默认页中间值按钮循环X/Y/W/H/SPLIT，正负键直接以0.5mm调整；宽高含义不再与INSET方向相反。
- **映射结果**：黄色框四角和红色分界线端点均通过完整A4单应矩阵正向映射，并由反向毫米映射测试逐点核对。
- **PC证据**：Round 1红灯17项预期失败；实现后定向78项、全量172项通过。
- **实机边界**：触摸可用性和真实纸面红线对齐仍需MaixCAM2验证。

---

## 2026-07-29 - A版拼装规划需求与代码基线

- **trace_id**：`maixcam2-a-assembly-20260729`
- **题目证据**：PDF第2页确认自备四片目标为图2固定布局，外框尺寸由 `2+8=10cm` 和 `2+1+3=6cm` 得到；随机的是上半区初始摆放。
- **未知题约束**：现场片不超过4片、每片不超过5边、每边不短于2cm、每片至少一条目标外边，目标矩形范围9×5cm～12×9cm。
- **自动化约束**：遮挡移除后一键启动，中途无人干预，正常室内光照，最长2分钟。
- **代码证据**：A版已经具备完整A4四角、逐点像素转毫米、四边形掩膜和最多四片全局模板匹配；缺少可调机械区域、毫米顶点、目标布局、未知求解和稳定帧缓存。
- **迁移判断**：A版设置必须升级到V3并兼容现有设备V2文件；B版继续保持V2和原发布包不变。
- **验证状态**：题目文字和PDF渲染已核对；算法实现尚待四轮TDD。

---

## 2026-07-29 - A版按CAL触发TypeError

- **trace_id**：`maixcam2-a-cal-interface-fix-20260729`
- **设备现象**：相机、显示和触摸初始化均成功，只有进入A版调参分支时在 `draw_calibration_frame()` 调用处退出，因此故障不在Maix多媒体驱动或触摸设备初始化。
- **三方证据**：A版源码、A版v1.1.0发布ZIP和PC端 `inspect.signature().bind()` 均复现 `paper_quad` 为不支持参数；B版ZIP调用不包含该参数。
- **根因模式**：调用方和被调用方接口不同步，而既有测试只直接调用绘制函数，没有覆盖主循环真实调用的关键字集合。
- **测试改进**：新增AST接口契约测试，解析真实入口并与实际函数签名比较，防止后续再次出现只在进入CAL分支时暴露的参数漂移。
- **处理结论**：删除两个冗余实参比扩展绘制接口更可靠，因为ROI预览已经从调参会话读取纸张四角和INSET，继续传参会形成重复数据来源。
- **验证状态**：PC端157项测试和A版v1.1.1平铺导入已验证；MaixCAM2上重新按CAL仍需用户实测。

---

## 2026-07-29 - A/B双版本最终PC验收

- **trace_id**：`maixcam2-auto-roi-ab-20260729`
- 稳定版65项与A/B版91项联合执行，共156项测试全部通过；测试覆盖单次自动定位、失败保留旧四角、1～4片锁定、A/B识别数据流、五页调参、设置分组保存、对比工具和发布包。
- `compileall` 与A/B源码包导入均以退出码0完成，说明PC侧Python语法和包内导入链成立。
- 两个发布ZIP重新生成后通过7项发布测试；其中两个平铺解压导入用例均通过，已覆盖MaixVision中直接运行 `main.py` 时的同级模块导入路径。
- 稳定版9个Python/YAML文件逐项SHA256完全一致，A/B开发没有改动 `maixcam2_app/`。
- 上述证据仅证明PC代码、合成场景和发布结构；相机曝光、真实黑纸反光、龙门架遮挡、触摸命中、运行帧率和重启恢复仍必须在MaixCAM2实机验证。

---

## 2026-07-29 - B版透视展开验证

- 完整A4四角映射到0～419、0～593像素边缘后，裁取 `y=67:527` 可得到精确420×460工作图，对应中间210×230mm。
- B版INSET在固定工作平面直接按2像素/mm生成有效掩膜；面积和白色占比使用掩膜非零像素数，不依赖相机透视面积。
- 碎片中心直接除以2得到机械工作区左上原点毫米坐标，完整A4的33.5mm裁剪偏移不再混入电机区域坐标。
- 420×460工作图显示到640×480时等比尺寸为438×480，左右各保留约101像素黑边，运行按钮仍使用640×480触摸坐标。
- PC证据：当前A/B累计55项测试通过，双变体编译和包导入通过；实际帧率与展开插值质量待实机验证。

---

## 2026-07-29 - A版四边形掩膜验证

- 仅使用四边形外接矩形仍会包含亮色三角角落，因此掩膜必须在开闭运算之后再次与四边形有效掩膜相交。
- A版面积阈值以 `contourArea(active_quad)` 为分母，白色占比以四边形有效像素数为分母，纸外像素不会稀释或放大诊断指标。
- 碎片完整性使用轮廓点到四边形边缘的有符号距离；小于等于 `border_margin_px` 时进入EDGE，不再依赖轴对齐矩形边界。
- A版保留原相机像素中心和轮廓，同时通过完整A4单应性附加 `center_mm`，便于后续机械坐标比较。
- PC证据：当前A/B累计44项测试通过，A版编译和包导入通过；实机透视和远距离边缘余量尚待验证。

---

## 2026-07-29 - A/B 实施前核对

- **trace_id**：`maixcam2-auto-roi-ab-20260729`
- A/B 目录和 `tests_ab/` 在实施前均不存在，稳定版是唯一业务源码。
- 根目录存在名为 `.git` 的普通目录，但 `git status`、`git rev-parse` 均返回 `not a git repository`，不能使用 worktree 或提交检查点。
- 稳定版全量基线为 `65 passed in 0.94s`，9个Python/YAML文件的SHA256已写入独立基线文档。
- 现有稳定版同时支持包导入和 MaixVision 平铺导入；A/B 复制后必须保留同级模块回退，但包导入名必须分别指向自己的变体。

## 2026-07-29 - 自动黑纸定位合成验证

- 反向Otsu生成暗色前景，外轮廓可保持完整纸边；纸内1～4片白色碎片表现为内部孔洞，不会切断外轮廓。
- 候选评分以A4短长边比例为最高权重，配合矩形度、凸性、画面面积和纸内暗度，在合成场景中正确排除外部细长暗杆。
- A/B两个定位模块在同一帧上返回等价四角、阈值和置信度；无A4和比例错误黑块均返回不带四角的失败结果。
- 物理有效区通过完整A4单应性计算，不直接按相机像素裁剪，因此透视场景仍能准确表达上下各33.5mm和四边毫米INSET。
- PC证据：Round 1共35项测试通过；现场真实材质、曝光和龙门架遮挡仍需实机验证。

---

## 项目信息

- **项目名称**：MaixCAM2 拼图碎片识别与现场调参
- **芯片平台**：MaixCAM2（MaixPy）
- **trace_id**：`maixcam2-calibration-20260729`
- **创建日期**：2026-07-29
- **最后更新**：2026-07-29

## 排查线索

### 2026-07-29 - 整张 A4 远距离识别为零

- **错误现象**：近距离黑纸占满画面时可识别，扩大到整张 A4 后结果变为零片。
- **根因分析**：整帧 ROI 包含亮色地面，亮地面成为 `RETR_EXTERNAL` 最外层轮廓，并因面积过大被过滤；黑纸内部白片不再作为外层轮廓返回。
- **本地证据**：合成回归中整幅 640 x 480 ROI 检出 0 片，只截取黑纸 ROI 检出 4 片。
- **处理结论**：使用固定 ROI 排除亮地面，并提供 `ROI/MASK/RESULT` 三页现场诊断。
- **状态**：已采纳，PC 合成测试已验证；实机画面待用户验证。

### 2026-07-29 - 形态学核参数边界不一致

- **错误现象**：手工参数文件可写入任意正奇数；调参状态机使用固定序列并通过 `values.index()` 定位当前值。
- **根因分析**：存储校验允许的值域大于 UI 状态机允许的值域，`OPEN=9` 或 `CLOSE=11` 会在进入调节路径时触发 `ValueError`。
- **处理结论**：先补失败测试，再把校验和 UI 选项统一到同一组常量。
- **状态**：已修复；新增 `OPEN=9`、`CLOSE=11` 拒绝用例并完成全量回归。

### 2026-07-29 - 发布物缺少调参运行模块

- **错误现象**：源码平铺导入测试已包含 7 个模块，但 `app.yaml` 和已有发布 ZIP 仍只有旧版 5 个业务模块。
- **根因分析**：Round 4 新增源码后没有同步应用清单和生成发布物。
- **处理结论**：应用清单增加 `settings_store.py`、`calibration_ui.py`，按明确文件列表重建 ZIP，并添加发布物回归测试。
- **状态**：已修复；应用清单和 ZIP 模块检查均通过。

### 2026-07-30 - KNOWN成功但UNKNOWN四片超时的几何根因

- **实机证据**：同一组默认四片在KNOWN模式能够规划；UNKNOWN日志中U1/U2含2.9～7.7mm伪短边，GRAPH只有9组连接，FOURFAST三片状态为0，FALLBACK以`size_reject=230`和`overlap_reject=261`为主并超时。
- **视觉根因**：`approximate_polygon()`从较小epsilon开始，第一次得到3～5顶点就返回，没有比较后续候选是否更符合题目真实边不短于20mm。
- **求解根因**：GRAPH调用`_solver_piece(..., clean_short_edges=True)`，FOURFAST与FALLBACK调用`False`；前者可能把五边形连续删成错误三角形，后两者则重新引入伪短边。
- **测试缺口**：既有测试明确断言三条路径使用不同清理策略，因此全量通过没有覆盖“统一几何口径”这个正确性要求。
- **已批准方向**：UNKNOWN完全不使用KNOWN模板；保留高保真完整轮廓用于硬验收，每片生成最多三个接缝候选，三条求解路径共享候选结构并在同一任务预算内按等级展开。
- **不采用措施**：不继续单独增大Beam、节点数或超时，不通过放宽92%/86%、3%重叠和尺寸门掩盖错误轮廓。
- **状态**：设计和实施计划已写入`docs/plans`，业务代码尚未修改。
## 2026-07-31 - CAL蓝框手动调节前置研究

- **trace_id**：`maixcam2-a-manual-paper-quad-20260731`
- **用户确认**：采用整体 `X/Y/W/H` 调节蓝色A4框；保留AUTO得到的透视形状；AUTO失败时优先沿用上次保存蓝框，没有历史框时生成居中A4比例初始框。
- **现有状态边界**：`paper_quad` 是蓝色完整A4框并由 `LOCK ROI` 持久化；`work_x_mm/work_y_mm/work_width_mm/work_height_mm` 生成黄色工作区，当前底部 `X 0.0mm` 调整的是黄色工作区而非蓝框。
- **界面约束**：A版CAL底部固定六槽，已经占用AUTO、减、参数、加、LOCK和SEND A4；手动功能应复用现有槽位，不能挤掉串口调试帧按钮。
- **默认关系**：AUTO成功或首次创建手动框时，将黄色工作区重置为完整A4，因此蓝框与黄框默认完全重合。
- **待设计确认**：ROI页单独负责蓝框AUTO/MANUAL及像素步进，MASK/RESULT继续负责黄色毫米工作区，可避免一条参数链循环十余项。
- **Round 1证据**：会话态使用独立`roi_mode/current_roi_item/manual_roi_step_px`，不升级JSON；无历史框时按当前PAPER方向在画面80%范围生成居中A4框。4项新增测试先因接口缺失失败，最小实现后聚焦文件`35 passed`，compileall与`git diff --check`退出0。
- **Round 2证据**：`X/Y`刚性平移四角；`W/H`分别绕上下边或左右边中点使用同一比例缩放，保持AUTO梯形的对应边方向。9项测试先因接口缺失失败；实现后聚焦13项和完整调参44项通过。
- **透视耦合边界**：真实梯形扩宽10px时平均高变化0.063px，来源是上下斜边长度不同；验收限制为另一维耦合小于0.1px，低于最小1px现场步进，不使用强制轴对齐来消除该正常投影效应。
- **Round 3证据**：ROI页六槽映射为`AUTO ROI / - / MODE-X-Y-W-H-STEP / + / LOCK ROI / SEND A4`；MASK/RESULT/MEASURE继续映射黄色毫米工作区。5项关键测试先按旧`work_*`路径失败，入口、模式回切和标签接入后相关三文件`85 passed`。
- **保存与失败边界**：AUTO成功把会话切回AUTO，AUTO失败保持MANUAL和当前蓝框；手动四角只在LOCK通过1至4片门槛后合并到运行设置。B版仍由共用参数化测试覆盖原五槽行为。
